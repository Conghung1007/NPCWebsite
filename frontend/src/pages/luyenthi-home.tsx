import { BlockPortalHome } from "@/components/BlockPortalHome";

/** Intro block layout for luyenthi portal `/` (exam list rendered below). */
export default function LuyenthiHome() {
  return <BlockPortalHome page="luyenthi" label="Luyện thi" />;
}
